# Generic analysis options where nothing is run
general:
    folder_output: outputs
    fname_output: M4Wtower_optim

design_variables:
    tower:                          # TODO
        outer_diameter:
            flag: True
            lower_bound: 6.5
            upper_bound: 10.0
        layer_thickness:
            flag: True
            lower_bound: 4e-3
            upper_bound: 1e-1
    # monopile:                     # TODO
    #     outer_diameter:
    #         flag: True
    #         lower_bound: 6.5
    #         upper_bound: 10.0
    #     layer_thickness:
    #         flag: True
    #         lower_bound: 4e-3
    #         upper_bound: 2e-1

constraints:
    tower:
        height_constraint:
            flag: False # TODO
            lower_bound: 1.e-2
            upper_bound: 1.e-2
        stress:
            flag: True
        global_buckling:
            flag: True
        shell_buckling:
            flag: True
        d_to_t:
            flag: False #False
            lower_bound: 120.0 # 90.0
            upper_bound: 2000.0 # 500.0
        taper:
            flag: False #True TODO
            lower_bound: 0.4
        slope:
            flag: True # TODO
        frequency:
            flag: False #False
        frequency_1:
            flag: False #True TODO
            lower_bound: 0.13
            upper_bound: 0.40
    # monopile:                         # TODO
    #     stress:
    #         flag: True
    #     global_buckling:
    #         flag: True
    #     shell_buckling:
    #         flag: True
    #     d_to_t:
    #         flag: False
    #         lower_bound: 120.0
    #         upper_bound: 2000.0
    #     taper:
    #         flag: False
    #         lower_bound: 0.2
    #     slope:
    #         flag: True
    #     thickness_slope:
    #         flag: True
    #     frequency_1:
    #         flag: True
    #         lower_bound: 0.13
    #         upper_bound: 0.40
    #     pile_depth:
    #         flag: True
    #         lower_bound: 0.0
    #     tower_diameter_coupling:
    #         flag: True

merit_figure: tower_mass # structural_mass #(tower+monpoile) # TODO

driver:
    optimization:
        flag: True
        max_iter: 20         # Maximum number of iterations (SLSQP)
        solver: SLSQP       # Optimization solver. Other options are 'SLSQP' - 'CONMIN'
        step_size: 1.e-6    # Step size for finite differencing (def.: 1.e-3)
        form: forward       # Finite differencing mode, forward or central (def.)
        debug_print: True